<!DOCTYPE html>
<html>
<head>
    <title>Edit <%= plant.name %></title>
</head>
<body>
    <h1>Edit <%= plant.name %></h1>
    <form 
        action="/plants/<%= plant._id %>?_method=PUT" 
        method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" value="<%= plant.name %>" required>
        <label for="type">Type:</label>
        <input type="text" name="type" id="type" value="<%= plant.type %>" required>
        <label for="waterNeeds">Water Needs:</label>
        <input type="text" name="waterNeeds" id="waterNeeds" value="<%= plant.waterNeeds %>" required>
        <button type="submit">Update Plant</button>
    </form>
    <a href="/plants/<%= plant._id %>">Back to Details</a>
</body>
</html>
